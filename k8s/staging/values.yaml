# Staging environment: new model versions are deployed here first for
# automated smoke tests and offline evaluation before canary traffic.

replicaCount: 1

scoringService:
  image:
    repository: ghcr.io/22navyakumar/aesthetic-scoring-service
    tag: latest
    pullPolicy: Always
  port: 8000
  modelUri: "models:/aesthetic-scorer/staging"
  mlflowTrackingUri: "http://mlflow.platform.svc.cluster.local:5000"

triton:
  enabled: true
  image:
    repository: nvcr.io/nvidia/tritonserver
    tag: "24.10-py3"
  port: 8000
  metricsPort: 8002
  modelRepository: "s3://http://minio.platform.svc.cluster.local:9000/triton-models/staging"
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 2000m
      memory: 4Gi

service:
  type: ClusterIP
  externalIP: ""

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
