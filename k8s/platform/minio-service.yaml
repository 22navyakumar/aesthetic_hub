apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: platform
spec:
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
<<<<<<< HEAD
  type: NodePort
>>>>>>> aca1f3d (Add Kubernetes manifests for Immich app and MinIO platform with persistent storage on Chameleon)
