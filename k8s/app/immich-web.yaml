apiVersion: v1
kind: Service
metadata:
  name: immich-web
  namespace: aesthetic-hub
  labels:
    app: immich-web
spec:
  type: NodePort
  selector:
    app: immich-server
  ports:
    - name: http
      port: 3000
      targetPort: 2283
      nodePort: 32080
      protocol: TCP
